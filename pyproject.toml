[project]
name = "mlx-infra"
version = "0.1.0"
description = "A hand-rolled MLX inference engine, built step by step for learning"
authors = [
    { name = "zack-szh", email = "919zack@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "huggingface-hub>=1.28.0",
    "mlx>=0.32.2",
    "mlx-lm>=0.31.3",
]

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
